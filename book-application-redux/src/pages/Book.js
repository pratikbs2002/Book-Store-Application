import React from "react";

export default function Book() {
  return <div   style={{
    width: "100%",
    height: "80vh",
    display: "flex",
    justifyContent: "center",
    alignItems: "center",
  }}>book</div>;
}
